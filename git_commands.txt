git commands:

git init
git add .
git commit -m "message"
git log
git revert {id}
git reset --hard {id}
git checkout -b "{new branch}"
git merge {feature branch} {main branch}
git branch -D {brnach want to delete}